# DiceManager 评估报告（2026-09-26）

> 范围：①上轮变更取消情况；②服务器占用与可瘦身点（含 MySQL 用途）；③新建骰子向导的直觉性
> 方式：只读取证 + 代码走查，**未对服务器做任何改动**

---

## 〇、上一轮变更已取消

上一轮计划"停止并删除宝塔面板"，实际只执行到只读取证阶段，**服务器上没有任何写入动作**
（未停服务、未删文件、未改配置）。本地两个只读诊断脚本已删除，`git status` 与取证前一致。

---

## 一、服务器占用评估

### 1.1 本项目（DiceManager）自身占用

| 项目 | 占用 | 评价 |
|---|---|---|
| 面板服务进程 | **RSS 35 MB**（峰值 36 MB，systemd 上限 400 MB） | 很健康，CPU 累计 1339s/天余，几乎空转 |
| `/opt/dicemanager` | 55 MB（venv 50 MB + 代码约 300 KB + 前端 dist 248 KB） | 正常 |
| **`/var/lib/dicemanager/packages`** | **266 MB** | **本项目最大占用项，无淘汰策略** |
| `/var/log/dicemanager` | 5 MB（8 个实例日志） | 偏小，但**实例删除后日志不回收** |

### 1.2 本项目可减小的地方（按收益排序）

**① 程序包缓存 266 MB —— 只增不减**
缓存了 5 个程序包，一次下载即永久驻留，无 TTL、无 LRU：

```
llbot    91 MB      olivadice  46 MB      napcat  29 MB
sealdice 83 MB      dicenext   19 MB
```

当前注册表只有 llbot / sealdice 两个实例在用，其余三个（dicenext + olivadice + napcat = **94 MB**）
是从未实际部署过的死缓存。建议：

- 立即可做：服务端提供"缓存管理"入口（目前前端只有针对单个程序的删除按钮），
  删除未被引用的包；或按 `updated_at` 做 TTL 淘汰（如超过 60 天且无实例引用自动清理）
- 更彻底：把程序包当作**构建产物**而非长期资产，实例稳定运行 N 天后自动清理其种子包

**② 生产环境 venv 里装了开发依赖（约 26 MB）**
`site-packages` 里 `pip` 13 MB + `pygments` 9.4 MB + `_pytest` 3.3 MB（含 pytest 本体）
都是运行面板所不需要的。服务器部署时只装生产依赖即可。

**③ 实例日志无回收（当前 5 MB，长期会涨）**
`/var/log/dicemanager` 里 8 个日志文件对应多个**已删除实例**（如 `sealdice-7bc03d98`、
`sealdice-b052fabb` 等孤儿日志）。删除实例时应一并归档或清理日志。

**④ `MemoryMax=400M` 相对实际用量过于宽松（可选）**
实际只用 35 MB，上限 400 MB 起不到"保护罩"作用。若希望防内存泄漏雪崩，可以下调到 128~200 MB——
但要注意留出上传大包解压时的峰值余量，改动需要压测确认。

### 1.3 服务器上的其他占用（非本项目，但收益更大）

**① 【强烈建议处理】失控的僵尸服务 `sealdice-manager.service`**

```
ExecStart = /opt/sealdice-manager/backend/venv/bin/python main.py   ← 该路径已不存在
状态：status=203/EXEC（找不到可执行文件），Restart=on-failure，RestartSec=5
NRestarts = 27709（截至取证时）
```

这是一个**早已失效的旧版管理器残留**：程序目录已被删除，systemd 单元还留着，于是
**每 5 秒重启失败一次**，过去 30 分钟贡献了 **1029/1603 条 journal（约 64%）**。
它是 852 MB journal 的主要来源，也在无谓地消耗 CPU 调度。

处理：`systemctl disable --now sealdice-manager.service` + 删除该 unit 文件。
它与当前运行的 `dicemanager.service` 是两个不同服务，停用不影响面板。

**② journal 日志 852 MB**
`journald.conf` 的 `[Journal]` 段为空，没有任何上限约束，日志文件从 6 月累积至今。
`journalctl --vacuum-size=200M` 可立即释放约 650 MB，并建议配置 `SystemMaxUse=200M`。

**③ APT 缓存 312 MB**：`/var/cache/apt` 112 MB + `/var/lib/apt/lists` 200 MB → `apt clean` 可清
**④ pip 缓存 58 MB**：`/root/.cache/pip` → `pip cache purge`
**⑤ mysqld RSS 174 MB**：见 1.4，停机即可释放本机最大的一笔常驻内存
**⑥ dockerd + containerd 约 55 MB**：`/opt/containerd` 仅 12 KB，若确认没跑容器可停用

> ⚠️ **不建议动的部分**：`/opt/QQ`（555 MB，疑似为 NapCat 准备的 QQ 客户端）与
> `/opt/sealdice` 下的 `lagrange`(86 MB) / `milky`(58 MB) 属于你手工放置的数据，非受管内容。
> 另注意 `/www/swap` 是宝塔创建的 1 GB 交换文件，将来若清理 `/www` 需先 `swapoff`。

### 1.4 MySQL 有什么用处？

**谁装的**：宝塔面板（二进制在 `/www/server/mysql`，数据目录 `/www/server/data`），
**不是本项目的依赖**。

**里面有什么**：除系统库外只有一个用户库，目录名 `@6211@7684@6570@636e@5e93`
（MySQL 对非 ASCII 库名做的十六进制文件名编码），解码后是中文 **「我的数据库」**，
另有约 13 MB 的 binlog。

**谁在用**：**没有任何服务在用**。取证结论：

- `ss` 显示 3306 **零个 ESTABLISHED 连接**（监听但无人对话）
- nginx 只挂载两个站点：`dicemanager.conf`（→ 面板，数据用 JSON 文件）和 `meme`（→ 图库 Python 服务）
- alist 的 `config.json` 明确 `"type": "sqlite3"`，用自己的 `data.db`
- DiceManager 的注册表就是 `/var/lib/dicemanager/instances.json`，纯 JSON，无数据库依赖
- 虽然装了 PHP，但没有任何 PHP 站点在 nginx 里生效

**结论**：它大概率是当初装宝塔时顺带启用、或者早期建站/测试留下的产物，
**停用不影响任何现有服务**，可释放 **174 MB 常驻内存**（这台机器总内存只有 1.6 GB，占比超过 10%）。
稳妥做法是先 `systemctl stop mysql` 观察几天业务无异常，再决定是否卸载；
万一将来要删宝塔，卸载脚本可能连带处理 MySQL，**建议先把 `/www/server/data` 打包备份一份**。

---

## 二、新建骰子向导的直觉性评估

**总体：6 / 10 —— 流程能用、有断点续跑等亮点，但把大量系统内部概念直接摆到了用户面前，
且默认路径容易造出一个"跑不起来"或"连不通"的实例。**

先列 Nietzsche，先说几条我认为成立的优点（这些应当保留）：
断点续跑（pending 列表 + 从中断处继续）、部署/上传双进度反馈（含"服务端落盘校验"文案，
避免了大包上传的假死感）、目录冲突二选一、AUTH TOKEN 前置到第一步 + 第三步兜底、
登录失败给出明确错误。这些都是踩过坑之后才有的东西。

下面按严重程度列出问题。

### P0-1 Step4「连接方向」默认 forward，对骰子端是**错的**

后端 `services/wizard.py:160`：

```python
default_port = (inst.allocated_ports or {}).get("ob11") or login_port or 3001
direction    = payload.get("direction") or inst.conn_direction or "forward"
```

问题在于 `direction` 兜底是写死的 `forward`。看 manifest 的实际能力：

| 程序 | 角色 | ob11 默认端口 |
|---|---|---|
| sealdice / dicenext / shiki | 骰子端（需搭配登录端） | **无** |
| llbot / napcat / lagrange / snowluma | 登录端 | 3001 |

也就是说**拥有 3001 端口的是登录端**。骰子端去连登录端，语义上应该是**反向 WS（主动连接）**，
但向导默认给的正向（自己监听等待别人来连）。若用户一路回车：

- 骰子端写 `connectUrl: ws://127.0.0.1:3001`（正向含义Self-listening）
- 登录端那边默认也是监听 3001 等连接

结果就是**两端都在监听，谁也不拨号** —— 这正是 9-25 那次「sealdice 连不上 llbot」事故的同源场景。

**建议**：方向默认值按实例角色推导——有 `ob11_default_port` 的（登录端）默认 `forward`；
没有的（骰子端，且关联了登录端）默认 `reverse`。UI 上不要让用户做这个技术选择，
而是给一句人话：**「海豹将主动连接 llbot（127.0.0.1:3001）」**，把高级选项收起来。

### P0-2 "不关联登录端"对 sealdice 是一条死路

`manifests/sealdice.json` 的 `compatible_login` 里包含 `builtin`（海豹内置 go-cqhttp 适配器），
因此 Step1 的登录端下拉给出默认值「不关联」看起来是合法的。但实测海豹 1.6.1 启动时会打：

```
ERROR 不支持或已经废弃的内置适配器模式
```

**这条内置路线在现代海豹上已经废弃**，也就是说新手按默认路径走完 5 步，会得到一个
永远连不上 QQ 的骰子，而向导全程没有任何警告。

**建议**：把 `builtin` 从 sealdice 的 `compatible_login` 移除，或标记为 experimental；
Step1 在"未选择登录端"时给出醒目提示：**「该程序需要搭配登录端才能收发消息」**。

### P1-1 首屏第一个控件就是"部署模式" —— 让用户在门口做架构决策

用户点"新建骰子"，第一个要回答的问题是：

> 部署模式：`先选骰子端（默认）` / `先选登录端 · 配对`

这是彻头彻尾的系统内部概念。真实用户的心智是「我要跑一个跑团机器人」，
他并不知道自己的机器人由"骰子端 + 登录端"两部分组成，更不知道谁先谁后。
更糟的是，两条路最终通向同一个结果，**但默认那条（先选骰子端）恰恰是容易踩坑的那条**
（见 P0-1/P0-2；配对模式反而自动保证了地址与 token 两端一致）。

**建议**：取消这个元选择，默认走配对式引导，把实际决策（"你已经有一个登录端了吗？"）
翻译成人话，并在用户选完骰子端后自动追问所需搭档；高级用户仍可在实例建好后手动解除关联。
如果一定要保留，也应把默认项反过来，并在 UI 里说明为什么推荐配对。

### P1-2 Step4 完全没有告诉用户"另一端是谁"

Step4 让用户填「地址（host:port，留空使用端口分配的默认值）」和「互联 Token」，
但页面上**看不到任何关于登录端的信息** —— 它是谁、它监听哪个端口、它用的什么 token。
用户面对 `127.0.0.1:3001` 这个占位示例只能靠猜。

而后端其实早就算好了 `login_port` / `login_token`（`_login_ref_info`），只是没展示出来。
**建议**：Step4 顶部画一个极简的两端示意图 + 一句人话结论，例如：

```
llbot-xxxx  (3001)  ──ws──▶  sealdice-xxxx
两端 Token 已自动对齐：s2W0…
```

既解释了在配什么，也让用户看到"系统已经帮我处理好了"。

### P1-3 Step3「登录」对骰子端是一次空转点击

STEP_NAMES 固定为 `['选程序','部署','登录','互联','启动']`，但 sealdice 的 `login_type`
是 `external`（自己不登录 QQ），于是 Step3 显示「该程序无需在此登录，点击继续」。
用户会困惑：**"我的骰子为什么要登录？"**，然后多按一次按钮，进度条还往前走了一格。

**建议**：步骤列表按该实例实际需要的环节动态生成（不需要登录就跳过"登录"这一格），
而不是对所有程序显示同样的 5 格。

### P1-4 Step1 信息过载 —— 一次塞进 6 类东西

部署模式、骰子程序、登录端关联、离线程序包上传、AUTH TOKEN、前置依赖说明，
全部堆在第一屏；其中最影响决策的信息（内存占用、是否需要外部登录端、要不要 AUTH TOKEN）
却都是一行灰色小字。`approx_memory_mb` 还只有 4 个程序有，展示时有时无，显得凌乱。

**建议**：Step1 只留"选程序"这一件事，并根据 manifest 自动生成一张
**「开始前请准备」清单**（需要手机 QQ 扫码 / 需要申请 AUTH TOKEN / 约 120 MB 内存 /
需要搭配 llbot 等登录端）；离线包上传移到第二步（它本质是"如何部署"而非"选什么"）。

### P1-5 Step5 没有就地反馈

点了启动后文案是「已下发启动命令，总览图出现新节点即完成」—— 用户不知道到底起来没有，
必须自己切走去看。而且很多程序在 Step3 扫码时已被 WebSocket 通道自动拉起，
这里的"启动"可能落到 `already_running`，体验上像没生效。

**建议**：Step5 直接就地显示进程状态（PID / 存活 / 运行时长）+ 最近几行日志，
并提供"查看日志"入口；已运行时显示"已在运行"而不是重复给启动按钮。

### P2 若干细节

- **步骤数会跳变**：配对模式下从 `第 1/3 步` 突然变成 `第 1/5 步`，进度感被打断，
  让 RP 用户误以为走回头路。建议统一用"阶段 + 进度"或始终显示总目标。
- **无法返回上一步**：互联写错只能走完，建议给非破坏性步骤提供"上一步"。
- **"整合包 / 独立程序"** 说法不直观（`arch === 'allinone'`），建议改成
  「自带登录端 / 需搭配登录端」。
- 术语建议面向用户改写：`互联` → `连接到 QQ`（或"接通消息通道"）；
  `正向/反向 WS` → 「由 XX 主动连接 YY」。

---

## 三、小结与建议优先级

| 优先级 | 事项 | 收益 |
|---|---|---|
| 高 | 停用僵尸服务 `sealdice-manager.service` | 消除持续写日志/重启，止损 |
| 高 | journal vacuum 到 200 MB + 配 `SystemMaxUse` | 释放约 650 MB 磁盘 |
| 高 | Step4 方向默认值按角色推导 | 消除"两端都不拨号"的连不通事故 |
| 高 | sealdice 移除/标记 deprecated `builtin` + 未关联登录端时告警 | 避免造出连不上 QQ 的空骰子 |
| 中 | 包缓存淘汰/清理入口 | 释放 94~266 MB |
| 中 | 生产 venv 不装开发依赖 | 释放约 26 MB |
| 中 | 实例删除时回收日志 | 防止长期增长 |
| 中 | 首屏取消"部署模式"元选择、Step1 出准备清单 | 显著降低新手门槛 |
| 低 | apt/pip 缓存清理、动态步骤名、返回上一步 | 细节体验 |

以上均为评估结论，**未执行任何改动**。需要我动手处理其中哪几项，说一声即可。
